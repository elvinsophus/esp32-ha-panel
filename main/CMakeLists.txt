idf_component_register(
    SRCS
        main.c
    INCLUDE_DIRS
        .
    REQUIRES
        esp_psram
        heap
        mqtt_service
        network
        nvs_flash
        ota
        profile
        runtime
        services
        ui)
