idf_component_register(
    SRCS
        main.c
    INCLUDE_DIRS
        .
    REQUIRES
        esp_psram
        heap
        network
        nvs_flash
        profile
        runtime
        services
        ui)
